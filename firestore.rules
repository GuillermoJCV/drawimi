rules_version='2'

service cloud.firestore {
    match /databases/{database}/documents {
        match /{document=**} {
            //TODO: LEARN FIREBASE RULES
            allow read, write: if request.time < timestamp.date(2025, 11, 26);
        }
    }
}
